{
  "name": "@subgate/bot-telegram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "tsx src/smoke.ts",
    "webhook:set": "tsx src/setup-webhook.ts set",
    "webhook:info": "tsx src/setup-webhook.ts info",
    "webhook:delete": "tsx src/setup-webhook.ts delete"
  },
  "dependencies": {
    "@subgate/types": "workspace:*",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2"
  }
}
